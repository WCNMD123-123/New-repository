const c=document.getElementById("sky"),ctx=c.getContext("2d");let W,H,stars=[];function size(){W=innerWidth;H=innerHeight;c.width=W*devicePixelRatio;c.height=H*devicePixelRatio;ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0)}size();addEventListener("resize",size);function ns(){return{x:15+Math.random()*(W-30),y:15+Math.random()*(H-30),r:.65+Math.random()*1.8,phase:Math.random()*6.28,speed:.0015+Math.random()*.003,age:0}}function draw(t){ctx.clearRect(0,0,W,H);stars.forEach(s=>{s.age=Math.min(1,s.age+.012);let w=.5+.5*Math.sin(t*s.speed+s.phase),a=s.age*(.12+.88*w);ctx.beginPath();ctx.arc(s.x,s.y,s.r,0,Math.PI*2);ctx.fillStyle=`rgba(255,249,214,${a})`;ctx.shadowColor="rgba(255,238,170,.95)";ctx.shadowBlur=2+7*w;ctx.fill();ctx.shadowBlur=0});requestAnimationFrame(draw)}requestAnimationFrame(draw);function explode(){for(let i=0;i<90;i++){let e=document.createElement("i");e.className="flash";e.style.left="50%";e.style.top="50%";let a=Math.random()*6.28,d=80+Math.random()*Math.max(W,H)*.7;e.style.setProperty("--dx",Math.cos(a)*d+"px");e.style.setProperty("--dy",Math.sin(a)*d+"px");document.getElementById("flash").appendChild(e);setTimeout(()=>e.remove(),1700)}}async function run(){try{const before=await(await fetch("/api/data")).json(),d=await(await fetch("/api/scan",{method:"POST"})).json();document.getElementById("stars").textContent=d.stars;document.getElementById("cur").textContent=d.stars;document.getElementById("target").textContent=d.target;document.getElementById("bar").style.width=Math.min(100,d.stars/d.target*100)+"%";setTimeout(()=>stars.push(ns()),250);if(d.stars>=d.target&&before.stars<d.target){document.getElementById("msg").textContent="星空已经被点亮！";explode()}}catch(e){document.getElementById("msg").textContent="网络连接失败，请稍后重试。"}}run();const box=document.getElementById("videoBox"),v=document.getElementById("video");document.getElementById("play").onclick=()=>{box.classList.add("on");v.play().catch(()=>{})};document.getElementById("close").onclick=()=>{v.pause();box.classList.remove("on")};